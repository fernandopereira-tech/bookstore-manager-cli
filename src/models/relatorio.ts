export interface RelatorioLivro {
  titulo: string;
  quantidade: number;
}

export interface RelatorioCliente {
  nome: string;
  quantidade: number;
}

export interface RelatorioStatus {
  status: string;
  quantidade: number;
}